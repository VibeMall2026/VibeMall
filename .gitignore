venv/
.venv/
__pycache__/
*.pyc
db.sqlite3
.env
bot/.env
# Timestamped copies made before editing the env files. They hold the same
# broker logins and passwords as the originals, and without this line a plain
# `git add .` would push every account credential to the remote.
.env.bak*
bot/.env.bak*
*.env.bak*
bot/sessions/
sessions/
_imports/fashionhub/
media/
staticfiles/
logs/
*.sqlite3
*.log
.pytest_cache/
.coverage
htmlcov/

# Node / frontend build artifacts
node_modules/
**/node_modules/
*.zip
vibemall---home/
vibemall---home.zip
