const root=document.getElementById('app');const params=new URL(location.href).searchParams;const state={apiBase:'',layout:{header:'',footer:''},orderNumber:params.get('order')||'',status:'',error:false,data:null};boot();async function boot(){root.innerHTML='<div class="loading">Loading order tracking tablet view...</div>';try{const layoutDoc=await fetchDoc('/shop/?bypass=true');state.layout.header=pick(layoutDoc,['header.vm-copy-header','.vm-copy-header','header']);state.layout.footer=pick(layoutDoc,['footer.vm-copy-footer','.vm-copy-footer','footer']);render();bind();if(state.orderNumber)await load(state.orderNumber);}catch(error){root.innerHTML=`<div class="error"><div><h1>Unable to load tracking view</h1><p>${safe(error.message)}</p><button id="retry">Retry</button></div></div>`;document.getElementById('retry')?.addEventListener('click',boot)}}function render(){root.innerHTML=`<div class="page"><div class="slot">${state.layout.header}</div><main class="main"><section class="shell"><form class="search" id="searchForm"><input id="orderInput" type="text" value="${safe(state.orderNumber)}" placeholder="Enter order number" required><button type="submit">Track Order</button></form><p class="status ${state.error?'error':''}" id="status">${safe(state.status)}</p>${state.data?renderData():`<div class="empty"><p>Enter an order number to fetch the latest backend tracking data.</p></div>`}</section></main><div class="slot">${state.layout.footer}</div></div>`}function renderData(){return`<section class="hero"><h1>${safe(state.data.orderNumber)}</h1><p>${safe(state.data.summary)}</p><div class="stepper">${state.data.steps.map((step)=>`<article class="step ${step.state}"><div class="step-dot"></div><span>${safe(step.label)}</span></article>`).join('')}</div></section><section class="cards">${state.data.facts.map((fact)=>`<article class="card"><strong>${safe(fact.label)}</strong><span>${safe(fact.value)}</span></article>`).join('')}</section><section class="items">${state.data.items.map((item)=>`<article class="item"><img src="${safe(item.image)}" alt="${safe(item.name)}"><div><h4>${safe(item.name)}</h4><p>${safe(item.meta)}</p></div></article>`).join('')}</section>`}function bind(){document.getElementById('searchForm')?.addEventListener('submit',async(e)=>{e.preventDefault();const order=document.getElementById('orderInput').value.trim();if(!order)return;await load(order)})}async function load(orderNumber){state.orderNumber=orderNumber;state.status='Loading order timeline...';state.error=false;state.data=null;render();bind();try{const doc=await fetchDoc(`/order/track/${encodeURIComponent(orderNumber)}/?bypass=true`);if(doc.querySelector('form[action*="/login"], .login-form-wrapper, .vm-login-mobile-only'))throw new Error('Please login first to view tracking details.');state.data=parse(doc);state.status='';history.replaceState({},'',`?order=${encodeURIComponent(orderNumber)}`);}catch(error){state.status=error.message||'Unable to load tracking right now.';state.error=true;state.data=null;}render();bind()}function parse(doc){const orderNumber=doc.querySelector('.order-number')?.textContent?.replace(/\s+/g,' ').trim()||state.orderNumber;const facts=Array.from(doc.querySelectorAll('.info-card')).map((card)=>({label:card.querySelector('.label')?.textContent?.trim()||'Info',value:card.querySelector('.value')?.textContent?.replace(/\s+/g,' ').trim()||''}));const timeline=Array.from(doc.querySelectorAll('.timeline-item')).map((item)=>({label:item.querySelector('.timeline-title')?.textContent?.replace(/\s+/g,' ').trim()||'Update',state:item.classList.contains('completed')?'completed':item.classList.contains('active')?'active':'pending'}));const items=Array.from(doc.querySelectorAll('.products-section .product-card')).map((item)=>({image:item.querySelector('img')?.getAttribute('src')||'https://via.placeholder.com/72x72?text=Item',name:item.querySelector('.product-name')?.textContent?.trim()||'Item',meta:item.querySelector('.product-meta')?.textContent?.replace(/\s+/g,' ').trim()||''}));return{orderNumber,summary:facts.map((f)=>f.value).slice(0,2).join(' • '),facts,steps:timeline.slice(0,5),items}}async function fetchDoc(path){let last=new Error('Request failed.');for(const base of bases()){try{const res=await fetch(new URL(path,base),{credentials:'include'});const text=await res.text();if(!res.ok&&res.status>=500)throw new Error(`Server error ${res.status}`);state.apiBase=base;return new DOMParser().parseFromString(text,'text/html')}catch(error){last=error}}throw new Error(`${last.message} Use ?apiBase=http://127.0.0.1:8000 if needed.`)}function bases(){const url=new URL(location.href);const explicit=url.searchParams.get('apiBase')||localStorage.getItem('vmApiBase')||'';const current=`${url.protocol}//${url.host}`;const fallback=['localhost','127.0.0.1'].includes(url.hostname)?`${url.protocol}//${url.hostname}:8000`:'';return[...new Set([explicit,current,fallback].filter(Boolean))]}function pick(doc,selectors){for(const s of selectors){const n=doc.querySelector(s);if(n)return n.outerHTML}return''}function safe(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;')}
